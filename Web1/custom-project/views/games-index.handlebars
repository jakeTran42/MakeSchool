<!-- games-index.handlebars -->

<h1>Game List</h1>

<a href="/games/new">Add New Game</a>

{{#each games}}
    <a href="/games/{{this._id}}">
        <h5>{{this.title}}</h5>
        <small>{{this.description}}</small>
    </a>
     <a href="/games/{{this._id}}/edit">Edit</a>
{{/each}}
